#include "main.h"
/**
 * mul - to  multiply two integers
 * @a: first integer
 * @b: second integer to multiply with the first one
 * Return: product of a and b
 * Description: multiplies two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
